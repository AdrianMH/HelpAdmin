@model IEnumerable<HelpAdminData.ProjectInformation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Project.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Project.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectInformationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectInformationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectInformationID })
        </td>
    </tr>
}

</table>
